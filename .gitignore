# Temporary files / local-only directories
temp_files/
archive/
tests/
docs/
scripts/

# Local config overrides + persisted state (see CLAUDE.md "Local overrides + persisted state").
# Normally lives under $env:ORIGINAL_SCRIPT_DIR outside the repo; this only matters for the
# dev-loop fallback (running the orchestrator directly, no launcher), which creates it here.
maintenance-persist/

# Self-update extraction + bootstrap download artifacts (created in the launch dir by script.bat)
script_mentenanta-master/
update.zip
pwsh.msi
*.msi
*.msix
*.msixbundle
*.tmp


# Log files
*.log

# PowerShell format/type definition files
*.ps1xml

# Backup files
*.bak
*.backup
*.old

# OS generated files
Thumbs.db
.DS_Store
desktop.ini

# IDE files
.vscode/
.idea/
*.suo
*.user

# Compiled files
*.dll
*.exe
*.pdb

# Test results
TestResults/
*.trx

# PowerShell modules cache
PSGetModuleInfo.xml
