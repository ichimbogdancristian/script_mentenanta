{
  "execution": {
    "shutdown": {
      "countdownSeconds": 120,
      "cleanupOnTimeout": true,
      "rebootOnTimeout": true,
      "rebootOnlyWhenRequired": true
    }
  },
  "modules": {
    "_comment": "skipSoftwareManagement/skipSystemConfiguration/skipDiskCleanup/skipWindowsUpdates gate a whole Type1+Type2 pair (checked by the orchestrator in Stage 2). skipSystemHealth and skipRestorePointManagement gate SUB-FEATURES of the SystemConfiguration pair (checked inside SystemConfigurationAudit): they turn off the health report and the restore point create/prune respectively, without disabling the rest of the configuration work. NOTE: skipRestorePointManagement true removes this run's rollback safety net AND can leave the SystemConfiguration diff empty, in which case Stage 3 skips the pair entirely.",
    "skipSoftwareManagement": false,
    "skipSystemConfiguration": false,
    "skipDiskCleanup": false,
    "skipWindowsUpdates": false,
    "skipSystemHealth": false,
    "skipRestorePointManagement": false,
    "systemConfiguration": {
      "_comment": "skipPasswordPolicy turns off the CIS 1.1/1.2 password + account-lockout rules (applied via secedit). Leaving it false enforces minimum length 14 and lockout after 5 bad attempts; the lockout auto-clears after 15 minutes, and a longer minimum length only applies at the NEXT password change, so an existing shorter password keeps working. Set true on machines where a lockout would be hard to recover from. skipAuditPolicy turns off the CIS 17.x advanced audit policy (applied via auditpol) - that one only increases Security-log volume and changes nothing about how anyone signs in.",
      "skipPasswordPolicy": false,
      "skipAuditPolicy": false
    },
    "softwareManagement": {
      "_comment": "aggressiveOemRemoval: when false (default), bloatware patterns tagged \"tier\": \"broad\" in bloatware-detection.json (whole-vendor wildcards like *Razer*, *ASUS*, Dell.*, HP.* that can also match software the user deliberately installed) are excluded from detection. Set true to include them.",
      "aggressiveOemRemoval": false
    },
    "windowsUpdates": {
      "_comment": "Lifecycle handling for OS versions that have fallen out of free servicing (see config/lists/windows-updates/os-lifecycle.json). autoAdvanceEolFeatureVersion (Windows 11 only): when true, sets Microsoft's documented TargetReleaseVersion policy to the latest known supported feature version so Windows Update itself offers and installs it through the SAME COM API path already used for regular updates - safe and unattended because it uses only a documented policy, never a hand-built download. attemptConsumerEsuEnrollment (Windows 10 only, default false): best-effort attempt to enroll in free Consumer ESU via the built-in ClipESUConsumer.exe tool. This is opt-in and NOT guaranteed to complete unattended - Microsoft's consumer ESU flow is designed around an interactively signed-in user/Microsoft Account, and this task normally runs as SYSTEM with no such session. The module always re-verifies eligibility afterward and reports Warning (never Success) if enrollment cannot be confirmed - enable only if you understand it may just be a no-op on most unattended runs.",
      "autoAdvanceEolFeatureVersion": true,
      "attemptConsumerEsuEnrollment": false
    }
  },
  "reporting": {
    "enableHtmlReport": true
  },
  "logging": {
    "_comment": "consoleLevel gates on-screen output; fileLevel gates maintenance.log. Levels: DEBUG < INFO/SUCCESS < WARN < ERROR < FATAL.",
    "consoleLevel": "INFO",
    "fileLevel": "DEBUG"
  }
}